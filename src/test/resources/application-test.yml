spring:

  sql:
    init:
      platform: h2

  jpa:
    generate-ddl: false
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:h2:mem:moro_rating_book_db_test
    username: sa
    password: ~